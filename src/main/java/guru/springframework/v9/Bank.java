package guru.springframework.v9;

public class Bank {

    /**
     * First impl just hardcode it
     * First impl just hardcode it
     */
    public Money reduce(Expression expression, String currency) {
        return Money.dollar(10);
    }
}
